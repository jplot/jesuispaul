<div class="container-fluid overflow-x-hidden" id="page-container">
    <%= render 'header' %>
    <div class="bento-stack">
        <div class="bento"><%= render 'home/lines/first' %></div>
        <div class="bento"><%= render 'home/lines/second' %><%= render 'home/lines/third' %></div>
    </div>
    <%= render 'footer' %>
</div>
