<div class="container-fluid overflow-x-hidden px-0" id="page-container">
    <div class="row">
        <%= render 'header' %>
        <div class="container-fluid p-0">
            <div class="row g-3">
                <%= render 'home/lines/first' %>
                <%= render 'home/lines/second' %>
                <%= render 'home/lines/third' %>
            </div>
        </div>
        <%= render 'footer' %>
    </div>
</div>